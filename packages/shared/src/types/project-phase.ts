/**
 * Project Phase Types
 * 
 * Defines the lifecycle phases of a consulting project.
 * Used for contextual hints and decision guidance (read-only).
 */

export type ProjectPhase = "discovery" | "design" | "delivery" | "review";

